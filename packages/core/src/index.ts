// @repo/core is consumed via the `@/lib/*`, `@/db`, `@/auth`, `@/types` and
// `@/config/*` path aliases configured in each app's tsconfig (and mirrored in
// this package's tsconfig for standalone type-checking). This entry point is a
// stub so the package has a resolvable main; import specific modules instead.
export {};
